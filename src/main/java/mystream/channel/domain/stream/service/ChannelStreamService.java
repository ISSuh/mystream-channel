package mystream.channel.domain.stream.service;

import mystream.channel.domain.stream.dto.StreamStatusDto;

public interface ChannelStreamService {

  void updateStream(StreamStatusDto streamStatusDto);

}
